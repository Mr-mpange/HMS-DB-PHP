# Laravel Backend API (Local Development)
VITE_API_URL=http://localhost:8000/api
VITE_SOCKET_URL=http://localhost:8000

# ZenoPay Configuration
# NOTE: ZenoPay API credentials are stored SECURELY in backend/.env
# Frontend calls backend API, which then calls ZenoPay
# This prevents exposing API keys in the browser
# Application
VITE_APP_URL=http://localhost:5173

VITE_PUSHER_APP_KEY=local-key
VITE_PUSHER_APP_CLUSTER=mt1
VITE_PUSHER_HOST=127.0.0.1
VITE_PUSHER_PORT=6001
VITE_PUSHER_SCHEME=httpwaa
